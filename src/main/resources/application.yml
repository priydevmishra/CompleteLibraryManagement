server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/librarymanagement?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Amaronki123qwe@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # options: none | validate | update | create | create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  # Optional: disable banner if you hate clutter
  main:
    banner-mode: console
